/* ============================================================
   HEALTHLINK — DOCTOR MODEL
   Defines the shape of a Doctor entity.

   {
     id:        number,
     name:      string,
     spec:      string,   // Specialty
     exp:       string,   // e.g. "12 years"
     rating:    number,   // 0–5
     patients:  number,
     fee:       string,   // e.g. "EGP 450"
     avatar:    string,   // initials
     available: boolean,
     bio:       string
   }
============================================================ */
